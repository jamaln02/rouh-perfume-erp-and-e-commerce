# If you plan to move to a Laravel backend, set the API base URL here
VITE_API_URL=
# Optional local Laravel proxy target when not using `php artisan serve`.
# VITE_DEV_API_PROXY_TARGET=http://localhost/rouh-app/public

# Optional: put feature flags or client-side keys here
# VITE_SENTRY_DSN=

# WhatsApp customer-service number is fixed by the application configuration.
